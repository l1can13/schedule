<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.schedule">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="com.schedule.files.READ_DATABASE"/>
    <uses-permission android:name="com.schedule.files.WRITE_DATABASE"/>

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Schedule">
        <activity
            android:name=".Files"
            android:exported="false" />
        <activity
            android:name=".Tasks"
            android:exported="false" />
        <activity
            android:name=".Add"
            android:exported="false"
            android:label="@string/title_activity_add"
            android:theme="@style/Theme.Schedule.NoActionBar" />
        <activity
            android:name=".Main"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Settings"
            android:exported="true" />
    </application>

</manifest>